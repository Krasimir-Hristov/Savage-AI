name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Lint, Typecheck, Test & Build
    runs-on: ubuntu-latest
    # Test-safe placeholder values — overridden by real secrets in the Build step
    env:
      NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: test-key
      SUPABASE_SECRET_KEY: test-secret-key
      OPENROUTER_API_KEY: test-key
      UPSTASH_REDIS_REST_URL: http://localhost:8079
      UPSTASH_REDIS_REST_TOKEN: test-token
      NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID: test-agent-id
      NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID: test-agent-id
      NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID: test-agent-id

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run TypeScript check
        run: npx tsc --noEmit

      - name: Run unit & integration tests
        run: npm run test:run
        # Inherits job-level env (test placeholders) — no per-step overrides needed

      - name: Build
        run: npm run build
        env:
          # Override job-level placeholders with real secrets for the build
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
          NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID }}
          NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID }}
          NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID }}

  e2e:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    needs: [ci]
    if: github.event_name == 'pull_request'
    # Test-safe placeholder values — real secrets are not needed for E2E infra checks
    env:
      NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: test-key
      SUPABASE_SECRET_KEY: test-secret-key
      OPENROUTER_API_KEY: test-key
      UPSTASH_REDIS_REST_URL: http://localhost:8079
      UPSTASH_REDIS_REST_TOKEN: test-token
      NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID: test-agent-id
      NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID: test-agent-id
      NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID: test-agent-id

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: npm run test:e2e
        # Inherits job-level env (test placeholders) — no per-step overrides needed
          NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_ANGRY_GRANDPA_AGENT_ID }}
          NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_BALKAN_DAD_AGENT_ID }}
          NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID: ${{ secrets.NEXT_PUBLIC_ELEVEN_LABS_CORPORATE_SHARK_AGENT_ID }}

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
